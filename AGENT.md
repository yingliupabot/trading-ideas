# AGENT.md — trading-ideas

在这个仓库里干活时必须遵守的约定。

## 回测规范
- 任何回测必须同时跑 baseline 对照（默认 buy-and-hold，同标的可比基准如 SPY）。
- 只报策略收益、不报 baseline 的回测结果视为未完成，不许交付。
- 回测报告里必须并列给出：策略收益 vs baseline 收益、超额收益、样本区间、交易次数。

## 词条（世界地图的数据）
- 真相只有一份：`词条/<id>/meta.json`。改完必须跑 `npm run index` 重新生成 `世界地图/词条.js`。
- 生成物要提交进仓库 —— GitHub Pages 是纯静态的，线上没有构建步骤。
- `status` 三态：`点亮`（懂了）/ `在读` / `想读`（知道它存在但还没碰）。
  **不懂的东西不要编内容进去**，标成「想读」留白即可 —— 空洞是这张图谱的一部分。
- `links` 指向别的词条 id，构建时会校验，断头链接直接让构建失败。
- **两类词条**：有 `place` 的是「事件」（上地球，必须有 `year`）；没有 `place` 的是「概念」（只在图谱与目录里占位）。
- **两个轴不要混**：`status` 是「我懂没懂」，`note` 是「写没写」。懂了但没写，和没懂，是完全不同的两件事。
- `note` 存**仓库根相对路径** —— 路径的含义不该取决于谁在读它。`世界地图/` 在子目录，读的时候自己补 `../`。
- `notes.html` 的章节表由 `npm run index` 在 `<!-- 词条:开始/结束 -->` 之间生成，**不要手改**。构建是幂等的，重复跑不产生假 diff。

## 时代
- 断代在 `世界地图/eras.js`，配色在 `世界地图/eras.css`（一个时代一种纸）。
- 边界是判断，不是事实。改了要知道它会影响哪些词条落在哪一段。

## 版面
- 顶栏和控制坞的高度是内容算出来的（时代带、时代注、筛选胶囊换不换行都会变）。
  **不要手写留白常数。** `timeline.js` 用 ResizeObserver 实测后写进 `--top-h` / `--dock-h`，
  地球直径、舞台留白、面板的上下边都从这两个变量推。
  同一个错犯过两次：坞一长高，地球下缘被压掉一截、面板叠到坞上。
- 改完版面跑 `npm run layout` —— 20 种视口逐个量地球、面板、控制坞会不会打架，
  有碰撞就退出码非零。

## 两个视图
- 全局视图看球,国家视图看一张平面地图。切换靠 `body[data-view]`,
  右舷那一列(`.tl-panel`)两个视图共用 —— 响应式、抽屉、断点不要再做第二遍。
- 地球换位置用 FLIP(量前后两个矩形,反向 transform 再放掉),
  不要写死坐标做动画:版面是响应式的,写死的值活不过下一次改版。
- 国家的平面地图是 `Globe.countryMap(name, w, h)`,等距圆柱 + 纬度余弦校正。
  它按**实测的纸张像素尺寸**算,所以必须等布局落定再调用。
- 改完卷轴或视图切换跑 `npm run country`。它验的是**铺开途中有没有中间态**,
  不只是首尾 —— 首尾对、中间是"啪"地一下全开,这个错犯过两次
  (clip-path 两端分量形状不一致;eras.css 的换色 transition 整条覆盖掉了它)。
